import { extendTheme } from "@chakra-ui/react";
import '@fontsource-variable/source-code-pro';

const theme = extendTheme({
    fonts: {
        nav: `'Source Code Pro', sans-serif`,
    }
})

export default theme;